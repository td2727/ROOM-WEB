<section class="services" id="services">
  <h2>NUESTROS SERVICIOS</h2>

  <div class="grid">
    <div class="card">
      <img src="/images/services/Periapical.png" alt="Periapical" />
    </div>

    <div class="card">
      <img src="/images/services/cefalometrica.png" alt="Cefalometría" />
    </div>

    <div class="card">
      <img src="/images/services/Panoramica.png" alt="Panoramica" />
    </div>

    <div class="card">
      <img src="/images/services/cefalica.png" alt="Cefálica" />
    </div>

    <div class="card">
      <img src="/images/services/tomo unitaria.png" alt="Tomografía unitaria" />
    </div>

    <div class="card">
      <img src="/images/services/tomo total y hemi.png" alt="Tomografía total y hemiarcada" />
    </div>
  </div>
</section>

<style>
  .services{
    background:#fff;
    padding:70px 16px 90px;
  }

  h2{
    text-align:center;
    font-size:56px;
    font-weight:300;
    margin:0 0 38px;
    letter-spacing:1px;
    color:#111;
    font-family: Georgia, "Times New Roman", serif;
  }

  .grid{
    max-width:1100px;
    margin:0 auto;
    display:grid;
    grid-template-columns:repeat(3, 1fr);
    gap:18px;
  }

  .card{
    height:310px;
    border-radius:14px;
    overflow:hidden;
    background:#111;
    box-shadow:0 12px 22px rgba(0,0,0,.12);
  }

  .card img{
    width:100%;
    height:100%;
    object-fit:cover;        /* CLAVE: llena el recuadro */
    display:block;
  }

  /* efecto suave al pasar el mouse (opcional, queda pro) */
  .card:hover img{
    transform: scale(1.04);
    transition: transform .4s ease;
  }

  @media (max-width:980px){
    h2{ font-size:44px; }
    .grid{ grid-template-columns:repeat(2, 1fr); }
    .card{ height:330px; }
  }

  @media (max-width:560px){
    h2{ font-size:34px; }
    .grid{ grid-template-columns:1fr; }
  }
</style>
